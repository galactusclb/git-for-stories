generator client {
    provider      = "prisma-client"
    output        = "../src/lib/prisma/generated"
    binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
    provider = "postgresql"
}